<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Room database (URL-independent progress). -->
    <include domain="database" path="." />
    <!-- DataStore settings (server URL) + catalog file cache. -->
    <include domain="file" path="datastore/" />
    <include domain="file" path="catalog-cache/" />
</full-backup-content>
